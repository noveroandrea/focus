import { SpriteSimulation } from './components/SpriteSimulation';

export default function App() {
  return (
    <div className="min-h-screen bg-slate-50">
      <SpriteSimulation />
    </div>
  );
}
